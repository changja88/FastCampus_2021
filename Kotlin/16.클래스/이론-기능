클래스의 기능
- 클래스 -> 거푸집
- 거푸집을 통해서 찍어내는 과정 -> 인스터화 한다
- 그 결과물 -> 객체, 오브젝트
    - val book = Book("책 제목")

- 거푸집을 사용 하는 방법 -> 생성자 사용법
- 결과물이 가지고 있는 것 -> 속성, 프로퍼티 (ex, 책 제목)

- 클래스 기능 추가 -> 클래스안에 함수(기능)를 추가한다



